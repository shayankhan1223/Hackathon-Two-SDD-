{{/*
Phase IV Todo Chatbot — Secret Template (Reference Only)
AI-Generated via Claude Code

NOTE: This template is intentionally left as a reference.
The actual secret should be created manually or via kubectl:
  kubectl create secret generic todo-db-secret \
    -n hackathon-dev \
    --from-literal=DATABASE_URL="<your-database-url>"

This prevents secrets from being stored in version control.
*/}}
{{/*
Uncomment below only if you want Helm to manage the secret:
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.secret.databaseUrlKey }}: {{ "" | b64enc | quote }}
*/}}
